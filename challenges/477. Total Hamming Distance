let totalHammingDistance = function (nums) {
    let total = 0;

    for (let i = 0; i < 32; i++) {
        let bitCount = 0;

        for (let num of nums) {
            bitCount += (num >> i) & 1;
        }

        total += bitCount * (nums.length - bitCount);
    }

    return total;
}
